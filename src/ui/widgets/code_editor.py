"""Compatibility shim for CodeEditor.

The implementation moved to `TPOPyside.widgets.code_editor` so it can be
shared across projects. Keep this module for backward-compatible imports.
"""

from TPOPyside.widgets.code_editor import *  # noqa: F401,F403
