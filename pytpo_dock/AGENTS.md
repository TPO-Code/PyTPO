# Rules
    keep code tidy and maintainable by using small files. if a file is large please mention and suggest refactoring.

# Gaps
    The background does not yet support transparency. we need to add a dock transparency setting an honor it (transparency for the field only and not the icons)

# Bugs
    setting "Choose how running window counts are shown under dock items -> instance indicator" Numbers, still shows the indicator as dots
    
    when the dock is first run, it shows and then a second later it gets wider
    
    when hovering over application icons  most of the time the previews are not shown, instead there is a small square with no content in it.